package application;

import java.time.Instant;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

public class Progam {

	public static void main(String[] args) {
		DateTimeFormatter dataAtual = DateTimeFormatter.ofPattern("dd/MM/yyyy");
		DateTimeFormatter HoraAtual = DateTimeFormatter.ofPattern("HH/mm/ss");
		
		LocalDate d1 = LocalDate.now();
		LocalDateTime d2 = LocalDateTime.now();
		Instant d3 = Instant.now();
		System.out.println("Data atual = "+ d1.get(dataAtual));
		System.out.println("Hora atual = "+ d2);
		System.out.println("data hora global = "+ d3);
		
		
		
		
		
		
		
		
		
	}

}
